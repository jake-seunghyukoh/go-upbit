package quotation

const BaseURL = "https://api.upbit.com/v1"

const TradeTicksURL = BaseURL + "/trades/ticks"

const CandleMinuteURL = BaseURL + "/candles/minutes"
const CandleDayURL = BaseURL + "/candles/days"
const CandleWeekURL = BaseURL + "/candles/weeks"
const CandleMonthURL = BaseURL + "/candles/months"
